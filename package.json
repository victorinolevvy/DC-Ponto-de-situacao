{
  "name": "dc-ponto-de-situacao",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:stack": "docker compose up --build",
    "dev:stack:down": "docker compose down",
    "seed:dev": "npm --prefix backend run seed:dev",
    "dev:fe": "npm --prefix frontend run dev -- --host 0.0.0.0 --port 5173",
    "dev:demo": "cross-env DATABASE_URL=file:./dev.db npm --prefix backend run prisma:generate -- --schema prisma/schema.sqlite.prisma && cross-env DATABASE_URL=file:./dev.db npm --prefix backend run prisma:push:sqlite && cross-env DATABASE_URL=file:./dev.db npm --prefix backend run seed:dev -- --schema prisma/schema.sqlite.prisma && concurrently \"cross-env DATABASE_URL=file:./dev.db npm --prefix backend run start:dev\" \"npm --prefix frontend run dev -- --host 0.0.0.0 --port 5173\""
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "wait-on": "^7.2.0"
  }
}
